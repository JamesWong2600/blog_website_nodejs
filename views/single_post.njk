<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/single_post.css">
</head>
<body>
    <div id="gg"></div>
    <div class="post-container">
        <div class="post-header">
            <h1 class="post-title">{{ blog_title }}</h1>
            <p class="post-meta">By {{ username }} on {{ time }}</p>
        </div>
        <div class="post-content">
            <p>{{ blog_content }}</p>
        </div>
        <div class="blog-image">
            <img src="/css/uploads/{{ image }}" alt="Post Image">
        </div>
    </div>

<div class="comments-section">
    <h2>Comments</h2>
    {% for comment in comments %}
    <article class="comment">
        <div class="comment-header">
            <span class="comment-username"><i class="fas fa-user"></i> {{ comment.username }}</span>
            <span class="comment-time"><i class="far fa-clock"></i> {{ comment.time }}</span>
        </div>
        <p class="comment-text">{{ comment.comment }}</p>
    </article>
    {% endfor %}
</div> 


    <div class="comment-input-container">
        <form action="/comment" method="post">
            <input type="hidden" name="blog_title" value="{{ blog_title }}">
            <input type="hidden" name="id" id="id" value="{{ id }}">
            <input type="text" name="comment" required placeholder="Add a comment..." class="comment-input">
            <button type="submit" class="new-post-btn">Comment</button>
        </form>

         
         </form action="/blog" method="get" style="display: inline;">
            <a href="/blog"><button type="submit" class="new-post-btn">back to blog list</button></a>
        </form>
    </div>

</body>
</html>